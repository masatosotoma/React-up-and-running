//Reducer function simple form
function myReducer(oldState, action) {
  const newState = {};
  // do something with `oldState` and `action`;
  return newState;
}

/*The reducer function is responsible for making sense of the reality 
when something happens in the world. The world is a mess, then an event happens. 
The function that should makeSense() of the world reconciles the mess with the new event 
and reduces all the complexity to a nice state or order: */

function makeSense(mess, event) {
  const order = {};
  // do something with mess and event
  return order;
}
