/*
Add a search feature to the Excel component that allows users to filter the contents of the table
*/

//1. Add a button to toggle the new feature on and off

//2. If the search is on, add a row of inputs where each one searches in the corresponding column

//3. As a user types in an input box, filter the array of state.data to show only the matching content